[Unit]
Description=coredns service

[Service]
User={{ coredns_user }}
Group={{ coredns_group }}
ExecStart={{ coredns_folder }}/coredns -dns.port=53
WorkingDirectory={{ coredns_folder }}
AmbientCapabilities=CAP_NET_BIND_SERVICE
